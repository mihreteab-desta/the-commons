<nav class="navbar">
    <div class="nav-container">
        <a href="/admin" class="nav-logo">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                <polyline points="9 22 9 12 15 12 15 22"></polyline>
            </svg>
            <span>The Commons</span>
        </a>
        
        <div class="nav-links">
            <a href="/admin" class="nav-link">Dashboard</a>
            <a href="/admin/users" class="nav-link">Users</a>
            <a href="/admin/categories" class="nav-link">Categories</a>
            <a href="/admin/reservations" class="nav-link">Reservations</a>
            
            <% if (user) { %>
                <div class="nav-dropdown">
                    <button class="nav-dropdown-btn">
                        <span class="user-avatar"><%= user.fullName.charAt(0).toUpperCase() %></span>
                        <%= user.fullName %>
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <polyline points="6 9 12 15 18 9"></polyline>
                        </svg>
                    </button>
                    <div class="nav-dropdown-content">
                        <a href="/auth/logout" class="logout-link">Logout</a>
                    </div>
                </div>
            <% } else { %>
                <a href="/auth/login" class="btn btn-outline">Login</a>
            <% } %>
        </div>
    </div>
</nav>